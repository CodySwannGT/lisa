# Plan: 1537-live-status-probe

Work item: `CodySwannGT/lisa#1537`

Flow: `Implement/Feature`

Target environment: `production`; configured remote base is `main`.

## Task metadata

```json
{
  "plan": "1537-live-status-probe",
  "type": "feature",
  "acceptance_criteria": [
    "Probe results structurally distinguish value, unknown with machine and human reasons, and not-applicable",
    "Throwing, failing, invalid, and timing-out probes degrade independently without fabricating a value or blocking siblings",
    "lisa ui exposes no-store same-origin JSON status transport and renders a fully usable page when every probe is unknown",
    "The reference GitHub auth probe reports a real value only when gh confirms authentication and otherwise preserves the correct unknown reason",
    "Fresh interactive browser evidence covers unauthenticated/all-unknown, thrown failure, timeout, and authenticated/live-value states"
  ],
  "relevant_documentation": "Issue #1537 and parent #1500 define the read-only tri-state transport; src/cli/ui-cmd.ts owns the loopback server; ui/index.html is the zero-build browser surface; PR #1668 makes the interactive browser controller implementation-neutral; the task-specific July 16 stash is recoverable WIP only and must be re-audited against current main.",
  "testing_requirements": [
    "Run the recovered focused unit and real-server endpoint suites before fixes",
    "For each newly discovered behavior gap, add a failing targeted regression before changing production code",
    "Run formatting, lint, typecheck, build, full unit, and integration gates",
    "Perform the Validation Journey interactively in a live browser; automated tests alone are insufficient",
    "Codify the live journey in the applicable regression runner after it passes"
  ],
  "skills": ["lisa-implement", "lisa-codify-verification", "lisa-usage-accounting", "lisa-drive-pr-to-merge"],
  "required_access": [
    {"tool":"GitHub repository/issues/PRs","status":"pass"},
    {"tool":"Git push, CI, auto-merge, deploy/release","status":"pass"},
    {"tool":"Interactive browser controller","status":"blocked","evidence":"Required in-app Browser discovery returned an empty list and getForUrl returned No browser is available"},
    {"tool":"Local loopback server and Bun test toolchain","status":"pass"}
  ],
  "verification": {
    "type": "browser-and-cli",
    "expected": "The live page renders explicit non-fabricated states for all scenarios, targeted and full quality gates pass, merged source is released, and the issue reaches status:done with evidence links."
  }
}
```

## Edge-case disposition

- ACCEPT: one probe throws or times out while healthy siblings return normally.
- ACCEPT: every probe is unknown and the full page remains rendered with reasons.
- ACCEPT: unsafe or non-JSON probe values become unknown rather than reaching JSON serialization.
- ACCEPT: the live browser journey may use injected deterministic probes; it must remain interactive and must not log the user's real GitHub session out.
- REJECT: automated Playwright tests alone do not satisfy initial browser evidence.
- REJECT: individual stack, CI, deploy, observability, automation, or version probes are sibling-ticket scope.
- DEFER: read-only control affordances belong to the sibling #1538 work unit.

## Effective completion condition

The verified change is merged to `main`, CI and deploy/release are successful, fresh remote/package evidence confirms the shipped status endpoint and browser surface, issue #1537 is terminal `status:done`, and the task-specific stash is removed only after its contents are safely committed and recoverable remotely. If the required Browser backend remains unavailable after code, review, CI, and deterministic endpoint gates pass, preserve the PR with auto-merge disabled and move the issue to terminal `status:blocked` plus `human-needed` with the exact unblock probe.
