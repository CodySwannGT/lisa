# Plan: sll-self-learning-loop-1548

Work item: CodySwannGT/lisa#1548 (prd-ticketed PRD; implementing its remaining OPEN descendants).
Flow: **Implement / Build** (features per pre-decomposed tickets; no bug reproduction).
Tracker: github. tracker_provider=github, work_item_ref=CodySwannGT/lisa#1548 (per-batch refs below).
Base branch: **main** (single environment; `deploy.branches = {production: main}`; ticket `dev` env values are placeholder enum — recorded assumption per #1561/#1564). Branch naming: `claude/1548-sll-<batch>`.
Roster: see `.lisa/roster.md` (sll-self-learning-loop-1548 section).
Runtime note: no TaskCreate/TaskList tools — this file is the task list; verification verdict at `.lisa/verification-status.json`.

## Binding constraints (from PRD + #1729 amendment)

- Ledger path ALWAYS via executable contract resolver (`@codyswann/lisa/learnings`), never hardcoded.
- Context budgets are HARD limits; all persistence PR-based; marker-dedupe idempotency throughout; headless-safe; **never block the build** (archaeology/reflection/history failures degrade gracefully); no learning-loops-about-learning; six-agent parity; `plugins/src/**` is source of truth (`bun run build:plugins`; `check:plugins`, `check:rules-pairing` gate CI).
- SLL-6 (#1556/#1569) closed-superseded. #1585 EXCLUDED (superseded by gardener #1735 — closing with comment). #1579 IN scope.
- Tickets currently carry stale `human-needed`/`status:blocked` labels from the pre-amendment park; on claim per batch: flip to `status:in-progress`, drop `human-needed`, comment that the /goal directive + #1729 amendment unblocked them.

## Effective completion condition (the contract Verify proves)

**End state**: every in-scope leaf below is CLOSED with `status:done`, each via a merged-to-main PR whose fix commit is a merge ancestor (`git merge-base --is-ancestor`); story/epic rollups (#1561→#1551, #1558/#1562/#1563→#1552, #1559/#1565→#1553, #1564/#1566→#1554, #1567→#1555) closed; #1585 closed-superseded.
**Proof command**: `gh issue list --repo CodySwannGT/lisa --state open --json number,title --jq '[.[] | select(.number >= 1551 and .number <= 1592)]'` returns `[]`, plus per-batch empirical proofs below surfaced in transcript/CI logs.
**Constraints that must hold**: existing default auto-merge behavior of git-submit-pr/drive-pr-to-merge not regressed (#1588 AC); `lisa doctor` behavior not regressed (#1582 AC); no build-blocking failure paths introduced anywhere (PRD footgun list); plugins sync + rules pairing checks stay green.

## Tool-access preflight (gate results recorded per probe)

| Tool | Probe | Status |
|---|---|---|
| GitHub (gh CLI, issues/PRs/labels/actions) | `gh auth status` + issue read (already exercised this session) | pass |
| bun toolchain (build:plugins, tests) | `bun --version` in worktree | pending — probe at batch start |
| worktree deps (node_modules for pre-push hooks) | `bun install` state check | pending — probe at batch start |
| Atlassian (JIRA changelog — #1571 validation) | acli auth status | **PASS via acli (2026-07-19)**: `acli jira auth status` → Authenticated, site geminisportsanalytics.atlassian.net, cody@geminisports.ai, oauth. Validation leg runs through the acli READ substrate (headless ladder tier 1) against that live site — READ-ONLY operations only (production JIRA; changelog reads of an existing backward-transitioned issue + an untransitioned issue; zero writes). ATLASSIAN_API_TOKEN/MCP still unavailable — curl+MCP tiers remain unproven; acli suffices for the #1571 journey. |
| Linear (#1573 validation) | Linear MCP live (list_teams etc.) BUT IssueHistory unreachable via MCP — needs LINEAR_API_KEY for GraphQL | **PARTIAL (2026-07-19)**: MCP pass; LINEAR_API_KEY UNSET → GraphQL history leg blocked unless provisioned; same break-out shape for the history-validation leg only |
| GitHub Actions (CI evidence) | via gh run list on PR | pass (implied by gh) |

Continuous gate: any tool need surfacing mid-flow gets probed immediately and recorded here + in the task row.

## Task list — 5 PR batches (phase order; Setup tickets first within each)

Legend: each task carries metadata inline. `relevant_documentation` filled by Explore before its batch builds. All tasks: `"plan": "sll-self-learning-loop-1548"`, `"skills": []` unless noted, `"learnings": []` (filled at task end incl. MLD-style notes), reviews = product+quality+coderabbit+spec-conformance in parallel, then learner.

### Batch 1 — SLL-1.2 finish: budgets into host CI (#1581) — branch `claude/1548-sll-b1-budget-ci`
- [ ] T1.1 (#1581, type:task) Ship budget check into host projects' CI via workflow templates.
  relevant_documentation: explorer-b1 bundle (in-session). Key: scripts/check-learnings-budget.ts:19-40 hardcodes paths (no resolver — must fix); contract caps in src/core/learnings-contract.ts:14-28; host CI = thin create-only ci.yml callers → reusable .github/workflows/quality.yml@main (+quality-rails.yml) — add ONE `learnings_budget` job there (idempotent by construction, reaches all hosts, satisfies AC c); precedent jobs verification_coverage (quality.yml:467-490) + e2e_coverage guard pattern (:496-533); invocation = new `lisa check-learnings-budget` CLI subcommand using readProjectConfig→resolveProjectLearningsFile (works for rails via bunx transient fetch, mirroring quality-rails.yml:320 npm -g ast-grep pattern); NO secrets on the new job (fleet standard); honor skip_jobs convention; #1561 AC requires entry-id in per-entry violation messages (verify src/core/learnings-entry.ts); missing file = pass-silent per contract; version-pinning trade-off (bunx floating latest) acknowledged in PR.
  AC: lisa apply installs a CI step running the budget check; over-budget learnings file fails host CI with the violated budget named; idempotent across re-applies; templates for all supported stacks.
  required_access: [{tool: gh, probe: "gh auth status", status: pass}, {tool: bun, probe: "bun --version", status: pending}]
  verification: {type: cli-test, command: "in a scratch host fixture run the installed workflow step against an over-budget PROJECT_LEARNINGS.md (path via resolver) and surface the failing exit + message; then within-budget → exit 0", expected: "non-zero exit naming violated budget; zero exit within budget; second apply produces no template diff"}

### Batch 2 — SLL-4 gate + persistence (#1588→#1589→#1590→#1591→#1592) — branch `claude/1548-sll-b2-gate-persistence`
  relevant_documentation (arch-b2 design, in-session): NEW agent plugins/src/base/agents/learning-judge.md (4-way taxonomy, hostile default, verdict schema w/ cited_evidence + confidence high|low) + NEW skill plugins/src/base/skills/lisa-persist-learning/SKILL.md (drop-note → PR-persist → provenance/consolidation) + thin command. skill-evaluator.md NOT touched (avoids #1734 ladder-router collision; #1729 lists SLL-4 persistence as composes-with). #1588: `auto_merge` input on lisa-drive-pr-to-merge (Inputs :13-34; gate step :76-103 incl. :90 `gh pr merge --auto` and :101-103 direct-merge fallback — BOTH conditional; new awaiting-human terminal in :218-227) + git-submit-pr hint threading (:11-16, :40); 4-cell matrix, default byte-identical. #1591 routing: high→normal auto-merge; low→auto_merge=false + `learning:needs-triage` label (create idempotently per drive-pr-to-merge:46-52 pattern; deliberately NOT human-needed to avoid lifecycle-scan entanglement). Markers: `<!-- [lisa-learning-drop] key=<fp> -->`, `<!-- [lisa-learning-pr] key=<fp> -->`, fp=sll4-sha1(normalize(rule)+triggering_issue)[:12]; body sections `## Provenance` + `## Consolidation decision`. R1 CRITICAL: learnings-writer.ts:79-82 append-only/dup-throw — add supersede-capable writer (keep single-arg signature back-compat; re-render + re-assert budget; never hand-edit markdown). R2: budget-CI redness on over-budget learning PRs is healthy interaction. R3: no eager assumptions — resolver only. R4: marker tags documented for gardener exclusion. DEVIATION from arch suggestion: one PR per batch (plan protocol), not per ticket — all 5 ACs evidenced on the batch PR.
  SEAM for batch 3 (#1586): learning artifacts = `[lisa-learning-pr]` marker + `learning:needs-triage` label grammar — capture streams exclude these.
- [ ] T2.1 (#1588) auto-merge-OFF mode for learning PRs (git-submit-pr/drive-pr-to-merge parameter; default path unchanged; repo-disallows-auto-merge must not silently direct-merge in off-mode).
- [ ] T2.2 (#1589) hostile-default judgment gate: 4-way taxonomy (durable-learning | one-off | misunderstanding/spec-gap | lisa-upstream), mandatory evidence citation, auditable record. Model: Fable-tier build lane.
- [ ] T2.3 (#1590, blocked by T2.2) dropped-with-reason note on triggering issue; writes nothing to surface; marker-deduped.
- [ ] T2.4 (#1591, blocked by T2.1+T2.2) PR-based persistence with confidence routing (high→auto-merge on via normal gates; low→off-mode + triage label); PR touches only the learnings surface; idempotent.
- [ ] T2.5 (#1592, blocked by T2.4) provenance chain + consolidation decision in PR body; consolidate-not-duplicate at write (via contract).
  verification (batch): {type: cli-test, command: "dry-run the gate on synthetic candidates (fluke, recurring, ambiguous-spec, lisa-fault) surfacing classifications; create a real low-confidence learning PR on a scratch branch with auto-merge off and show it waits; re-run shows no duplicate PR/note", expected: "4 classifications correct per AC; off-mode PR open+unmerged with triage label; idempotent re-run"}

### Batch 3 — SLL-2 rejection reflection (#1571,#1573 → #1575 → #1586,#1587) — branch `claude/1548-sll-b3-rejection-reflection`
  relevant_documentation (explorer-b1 bundle 2): #1572 SHIPPED precedent = PR #1667 / commit 474c29a27 (widened timelineItems to LABELED_EVENT/UNLABELED_EVENT + pagination while-loop + `## Label-Event History` block + unknown-on-failure + test + build:plugins fan-out) — mirror for JIRA/Linear. Atlassian substrate: 3-tier dispatch table lisa-atlassian-access/SKILL.md:287-313 (add `changelog` READ row via `?expand=changelog`; MCP cell likely "(not exposed)"; annotate `transitions` row :294 as available-not-past false-friend; headless ladder = acli→curl+token per :359-361). Linear: add `history` op to Invocation Contract (SKILL.md:15-32) via existing linear_graphql() adapter (:60-82) — issue.history{fromState,toState,actor,createdAt}; MCP cannot reach IssueHistory. Detection seam = top of step 3b in all three build-intake skills (github :253-266, jira :197-212, linear :187-199 — 3b claim relabel is the idempotency lock; signal dies after relabel). #1575 ships as ONE shared eager+reference rule pair (leaf-only-lifecycle precedent), cited by all three arms; label lanes from src/sync/registry.ts:73-89 defaults via config, never hardcoded; Linear detection keys off status:* LABEL history (queue is label-driven per lisa-linear-build-intake:72-74). Marker grammar: `<!-- [producer-tag] key=<stable-key> -->` (github-write-prd Phase 2 discipline, match marker never title); #1586 candidate key = issue + backward-transition timestamp; SEAM DEPENDENCY: #1586 trigger-side exclusion consumes the learning-artifact marker/label batch-2 mints — confirm with arch-b2 design before building #1586.
- [ ] T3.1 (#1571) JIRA changelog transition-history op on lisa-atlassian-access (empty-not-error; substrate contract; failure never blocks build). Gate: Atlassian probe.
- [ ] T3.2 (#1573) Linear transition-history op on lisa-linear-access (same contract). Gate: Linear probe.
- [ ] T3.3 (#1575, blocked by T3.1/T3.2 + shipped #1572) vendor-neutral backward-transition detection contract (JIRA+GitHub+Linear; forward-only and never-left-ready not flagged; failing query → "unknown" and build proceeds).
- [ ] T3.4 (#1586, blocked by T3.3) read rejection evidence at re-claim → candidate into gate with provenance; own learning artifacts never failure signals; idempotent.
- [ ] T3.5 (#1587, blocked by T3.3) re-implementation consumes rejection evidence (plan addresses it; doesn't re-propose rejected approach; absence never blocks).
  verification (batch): {type: cli-test, command: "on the live GitHub tracker: take a scratch issue through review→ready backward transition, run detection surfacing the classification; run re-claim reflection producing one candidate with provenance; re-run for idempotency; JIRA/Linear ops proven against real trackers or formally blocked per tool-access-gate", expected: "backward transition detected exactly once with evidence; forward-only control not flagged; candidate carries rejection provenance"}

### LEAD DECISIONS (cross-batch contracts — binding on batches 3-5)
1. **Learning-artifact exclusion registry** (consumed by #1574 ancestor-exclusion, #1586 trigger-guard, #1583): an artifact is a "learning artifact" iff it carries any of: `<!-- [lisa-learning-drop] ... -->`, `<!-- [lisa-learning-pr] ... -->`, `<!-- [lisa-learning-upstream-handoff] ... -->` (batch-2 markers, SHIPPED in b2 branch), the `learning:needs-triage` label, or the SLL-5 upstream producer marker `<!-- [lisa-upstream-attribution] key=<lisa-surface-root-cause-key> -->` (minted by #1583; key derives from the Lisa surface at fault, NEVER the host project/local issue). Such artifacts are never ancestors, never rejection-reflection triggers, never evidence candidates.
   BATCH-2 BUILT (2026-07-19): branch claude/1548-sll-b2-gate-persistence in worktree /Users/cody/workspace/lisa/.claude/worktrees/agent-ae46a6c241a062936, commits a9cf0f8c8/c35a9a555/ab2846566/737c4404c; gates green (4648 unit, check:plugins in sync); fingerprint formula sll4-sha1(normalized-rule+"\n"+triggering_issue)[:12]; persistConsolidatedLearning(projectRoot, candidate, {supersede}) added, persistLearningEntry delegates back-compat; command flat at commands/persist-learning.md (no lisa/ subdir — repo convention). Reviews pending.
2. **#1579 seam timing**: detection happens POST-implementation within the intake cycle (end of build-intake step 3c, when the diff exists and "demonstrably applied" is observable — plan/diff cites the entry's rule), write is the net-new surgical `confirmLearningEntry(projectRoot, id, date)` op in learnings-writer.ts (same lock/safety/atomic machinery; advances last_confirmed only; idempotent within a claim; never blocks).
3. **#1584 config**: Pattern A — skills read `.lisa.config.json` `archaeology.*` via jq with documented defaults (per lisa-linear-access:42 precedent); optionally seeded via SYNC_REGISTRY with no artifact binding. No CI-threshold artifact file.

### Batch 4 — SLL-3 archaeology (#1570 → #1574 → #1580, #1584) — branch `claude/1548-sll-b4-archaeology`
  relevant_documentation (explorer-b1 bundle 3): #1570 gate = ONE shared eager+reference rule pair (repo-scope-split/leaf-only-lifecycle precedent) cited by all three build-intake arms at the 3a→3b window (real GitHub 3b header :253, not ticket's ~L244); #1574 signals: (a) typed relations already parsed by read skills (github-read-issue :108-115, :265-268; no "introduced by" field exists — inferred semantic), (b) text similarity has ZERO precedent — build on tracker search primitives only (jira search-issues jql, linear list-issues, gh search issues), scope to "recently-closed touching same files ranked by title/label overlap", (c) git ancestry via direct deterministic commands (git log --follow / blame / --grep "Merge pull request #"), NOT via git-history-analyzer agent (prose output, no machine contract); exclusion registry per LEAD DECISION 1. #1580 classification fresh|retry-of-done|rejection-reclaim reuses batch-3 detection signal. #1584 budget per LEAD DECISION 3; degrade-to-fresh; budget configurable.
- [ ] T4.1 (#1570) archaeology gate seam between claim steps 3a/3b in all three build-intake flows; one shared rule slug; pairing-valid eager+reference pair; gate failure never blocks.
- [ ] T4.2 (#1574, blocked by T4.1) ancestry search: tracker metadata links, text similarity vs recently-closed, git blame/history; learning PRs never ancestors; deterministic git ancestry. Fable-tier lane.
- [ ] T4.3 (#1580, blocked by T4.2) classify fresh | retry-of-done | rejection-reclaim (+reuse SLL-2 signal); candidate cites the done-vs-needed delta. Fable-tier lane.
- [ ] T4.4 (#1584, blocked by T4.2) hard cost budget (configurable), degrade-to-fresh, never strand a claim, idempotent re-claims.
  verification (batch): {type: cli-test, command: "seed scratch issues: A closed-done + B filed referencing A's shipped defect; run the gate at claim of B surfacing ancestor=A and classification=retry with delta; run on a fresh control issue surfacing fresh/no-candidate; kill the search budget artificially and surface degrade-to-fresh", expected: "retry classified with A cited; fresh control clean; budget breach degrades without blocking claim"}

### Batch 5 — SLL-5 attribution + #1579 (#1582 → #1583; #1579) — branch `claude/1548-sll-b5-attribution`
  relevant_documentation (explorer-b1 bundle 3): #1582 extraction target is SKILL PROSE not TS — lisa-doctor/SKILL.md "Upstream Lisa change-history diagnosis" :294-377 (shipped dff7b0d5e + pagination 3a662e555; version-window compare via gh api compare/v<installed>...v<latest>; attribute step :372-377 already bidirectional). Generalize: new shared skill accepting an arbitrary failure event {defect, implicated files, rule/skill/hook} → verdict lisa|project|ambiguous + cited evidence, with version-window compare demoted to signal 3 of 3 (managed-file check, shipped-rule check, upstream-history check); lisa-doctor must delegate to it regression-free. #1583 models on lisa-rework-triage "Filing upstream" :112-127 (dedupe-first, three-audience, self-hardening label, hardening.upstreamRepo config default CodySwannGT/lisa) UPGRADED to marker-dedupe (github-write-prd Phase 2); producer marker per LEAD DECISION 1 (key = Lisa root-cause surface); per-run cap per repair-intake max_candidates precedent (:77,86,:300-302), dropped candidates noted; ambiguous files NOTHING; local note links ticket only (no durable rule). #1579 per LEAD DECISION 2 (surgical confirmLearningEntry; contract fields learnings-contract.ts:14-28, ISO date + last_confirmed>=first_learned invariant learnings-entry.ts:33-35; persistLearningEntry append-only confirmed :80-82).
- [ ] T5.1 (#1582) generalize doctor upstream-history attribution into reusable event-triggered procedure (Lisa-managed-file evidence → LISA; project-owned → PROJECT; inconclusive → local low-conf; doctor not regressed). Fable-tier lane.
- [ ] T5.2 (#1583, blocked by T5.1) auto-file upstream Lisa ticket: marker dedupe, evidence-rich body, per-run cap, ambiguous files nothing, local note links ticket (no durable local rule).
- [ ] T5.3 (#1579) bump last_confirmed when a learning demonstrably applied at claim (presence ≠ applied; idempotent within claim; never rewrites entry; failed bump never blocks). Uses contract resolver/writer.
  verification (batch): {type: cli-test, command: "synthetic failure in a Lisa-managed template file → attribution surfaces LISA verdict + evidence and files exactly one upstream ticket (re-run: updates, not duplicates; cap enforced); project-file control → PROJECT, nothing filed; demonstrably-applied learning → last_confirmed bumped once, shown via contract read", expected: "verdicts and single-ticket dedupe as stated; bump exactly once"}

### Closeout (after Batch 5)
- [ ] C1 Close story/epic rollups with evidence comments; PRD #1548 rollup comment; lisa-usage-accounting entries per batch on the work items; tracker-sync done labels (`status:done` — production env key); ancestry check per merged PR.

## Batch protocol (every batch)
1. Claim: flip leaf labels ready→in-progress, drop human-needed with unpark comment.
2. Explore research → fill relevant_documentation rows here.
3. Architecture pass (batches 2–5).
4. Builder TDD (model floor Opus 4.8; Fable lanes as marked) on the batch branch off origin/main.
5. Quality gates + parallel reviews (product, quality, coderabbit, spec-conformance) → fix valid findings.
6. verification-specialist: empirical batch proof → verification-status.json (never the implementer).
7. learner: capture learnings (ledger-candidate notes; no unilateral promotions given this session's #1729 direction).
8. Commit batches, push, git-submit-pr (target_branch=main, work_item_refs= batch leaves), drive-pr-to-merge, ancestry verify.
9. tracker-sync each leaf: pr-merged → status:done + close; update this file's checkboxes.
