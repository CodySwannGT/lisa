# Plan: improve-harness-skill-1744

Work item: CodySwannGT/lisa#1744 — New skill `lisa-improve-harness` (+ `/lisa:improve-harness` command): bounded baseline→intervention→fresh-rerun loop with gap taxonomy.

Flow: Implement / Build (childless Story leaf; claimed via build-intake in this session).
Base branch: `main` (Target Backend Environment: production → deploy.branches.production = main). PR targets main.
Branch: `1744-lisa-improve-harness`.
Runtime: Claude Code implicit-team (no TaskCreate) — this file is the task list. Roster: see roster.md entry `improve-harness-skill-1744`.

## Effective completion condition

All of:
1. `bun run check:plugins` exits 0 on the branch (committed artifacts match regenerated source) — proof command: `bun run check:plugins; echo EXIT=$?` → `EXIT=0`.
2. The built plugin exposes the skill and command on every agent surface the build fans out to — proof command: `ls plugins/lisa/skills/lisa-improve-harness/SKILL.md plugins/lisa/commands/improve-harness.md` (flat base command; per-agent variants use commands/lisa/) → all paths exist.
3. The SKILL.md satisfies the 4 Gherkin AC by inspection mapped to sections (spec-conformance matrix all-covered): gap-classification-precedes-intervention (taxonomy + evidence citation + worker-limitation multi-failure guard), relevance gate (no-evidence-for-intervention outcome), bounded authority (proposed-intervention ticket + stop), result record posted to originating item (retain/revise/remove/test-without).
4. Quality gates green: repo lint/typecheck/tests pass locally and in PR CI.
5. PR merged into main (ancestry-verified), issue #1744 relabeled status:done and closed (terminal env), evidence comment posted.
Constraints: no changes to generated plugins/lisa* except via `bun run build:plugins`; no unrelated working-tree changes staged (.lisa.workspaces.json untouched); no behavior changes to existing skills.

## Tool access preflight (tool-access-gate)

- gh CLI authenticated (probe: `gh auth status` → logged in as CodySwannGT) — PASS
- git push access to CodySwannGT/lisa (probe: fetch origin succeeded; push proven at PR step) — PASS (fetch)
- bun 1.3.11 + build:plugins/check:plugins scripts present (probe: `bun --version`, jq scripts read) — PASS
- No remote runtime/deploy tooling needed: deliverable is repo-internal (skill + generated artifacts); "deploy" is the npm release automation post-merge. No AWS/Figma/Sentry/etc.

## Tasks

### T1 — Research conventions and adjacency (Explore: researcher-1744) — DONE
Key findings folded into relevant_documentation: peer skeletons = attribute-failure/rework-triage/persist-learning (frontmatter name+description+allowed-tools; Input/Output/Phase/Rules; inline fenced templates); command = flat `plugins/src/base/commands/improve-harness.md` (description+argument-hint frontmatter, one-line passthrough body with $ARGUMENTS); fanout is directory-driven — NO manifest/marketplace/count-test edits needed; skill-frontmatter-contract test validates new files dynamically (description ≤1024 chars, quoted if ": "); check:plugins = rebuild-and-diff gate; NO existing repo vocabulary for gap taxonomy (define inline); rules to cite by slug: verification, empirical-inquiry, project-learnings, upstream-to-lisa, promotion-contract, tool-access-gate; cross-invoke lisa-attribute-failure + lisa-persist-learning + lisa-tracker-write rather than duplicating; marker syntax precedent: HTML-comment `<!-- [lisa-improve-harness] key=<fingerprint> -->` with visible prose line, sha1-12 fingerprint recipe.
```json
{"plan":"improve-harness-skill-1744","type":"task","acceptance_criteria":["Dense report: skill conventions, command wiring, fanout/parity requirements, overlap map, taxonomy grounding, tests with hardcoded counts"],"relevant_documentation":"","testing_requirements":[],"skills":[],"learnings":[],"required_access":[{"tool":"local repo (read-only)","probe":"n/a","status":"pass"}],"verification":{"type":"documentation","command":"report returned to lead and folded into T2 design + this plan's relevant_documentation","expected":"report covers all 7 requested sections with file paths"}}
```

### T2 — Architecture: skill structure + parity design (lisa:architecture-specialist) — DONE
Design at `.lisa/design-1744-improve-harness.md`. Key decisions: (a) Verdict (evidence status: intervention-supported | no-evidence-for-intervention | insufficient-evidence | unclassified | bounded-authority-stop) split from Decision (retain|revise|remove|test-without) so AC2 and AC4 both hold; (b) headless proposes-only — smallest-owning-change mutation is interactive-mode authority, on branch harness/<fingerprint> via git-submit-pr, never default branch. Marker: `ih1-<sha1-12>` fingerprint, visible prose + HTML comment, 5-step dedupe. worker-limitation guard: ≥2 comparable cited trajectories, else degrade to best-supported-of-six or `unclassified` terminal.
```json
{"plan":"improve-harness-skill-1744","type":"task","acceptance_criteria":["Section-by-section outline of SKILL.md covering all 4 AC scenarios + cross-cutting invariants","Command file spec","Exact file list to create/regenerate incl. any manifest/test updates","Idempotency marker syntax decision","Overlap boundaries vs attribute-failure/rework-triage/debrief recorded"],"relevant_documentation":"<-T1 report","testing_requirements":[],"skills":[],"learnings":[],"required_access":[],"verification":{"type":"documentation","command":"design doc returned; lead checks every AC scenario maps to a planned section","expected":"4/4 AC mapped; parity surface list complete"}}
```

### T3 — Build: author skill + command, regenerate artifacts (lisa:builder) — DONE
Commit f5d571745 (13 files, +2492: src pair + all regenerated artifacts atomically). build:plugins exit 0; check:plugins exit 0 (post-commit — it requires a clean plugins/ tree); 302 tests passed across 6 relevant test files. Artifacts confirmed on all surfaces (lisa, .codex-plugin, cursor/agy/copilot). Builder MLD captured for learner: (1) check:plugins must run AFTER committing the src+artifact pair — refuses dirty tree; (2) lint-staged prettier --write at commit time can desync src from generated artifacts — re-verify check:plugins post-commit; (3) one base skill+command fans to 13 files.
```json
{"plan":"improve-harness-skill-1744","type":"story","acceptance_criteria":["plugins/src/base/skills/lisa-improve-harness/SKILL.md implements the loop with embedded job-contract + result-record templates, gap taxonomy, relevance gate, bounded authority, idempotency marker, headless-safe mode","command file added","bun run build:plugins run; artifacts committed","any manifest/count tests updated","conventional commits via git commit -F with Co-authored-by: Claude trailer"],"relevant_documentation":"<-T1,T2","testing_requirements":["bun run check:plugins green","existing test suite green (bun test scope relevant to plugins scripts)"],"skills":["lisa:lisa-git-commit"],"learnings":[],"required_access":[{"tool":"bun","probe":"bun --version","status":"pass"}],"verification":{"type":"cli-test","command":"bun run check:plugins; ls plugins/lisa/skills/lisa-improve-harness/SKILL.md plugins/lisa/commands/lisa/improve-harness.md","expected":"check:plugins exit 0; both artifact paths exist"}}
```

### T4 — Parallel reviews (product, quality, coderabbit, spec-conformance) — DONE
product: NEEDS_CHANGES (2 blockers: silent terminal exits, operator jargon in record; 4 majors: test-without dead end, headless verdict collision, dropped contract fields, no change link). quality: NEEDS_CHANGES (2 critical: no Verdict for failed reruns, observed_failure duplicate filing; 5 warnings incl. missing contract test, allowed-tools "Agent" nonportable). coderabbit: NEEDS_CHANGES (4 high: TTY headless trigger makes interactive unreachable, missing verdict, Phase7/8 contradiction, fingerprint keyed on item_ref breaks self-filed items; 5 medium; 3 low; branch 4 behind main → rebase). spec-conformance: PARTIAL — 100% textual AC/invariant coverage, zero scope creep; flips CONFORMS on T6 empirical proof.

### T5 — Implement consolidated review findings (lisa:builder) — DONE
All 21 findings implemented, none judged invalid. Rebased (autostash) onto ebe18cd90 (2.249.0); original replayed as 45a508712; fix commit b34915968 (12 files, +1171/−545 incl. new test). check:plugins exit 0 post-commit; 167 files / 3737 tests green; new suite 29/29; typecheck clean. Anchoring deviation (form only): marker anchor is `key=<fingerprint> ` incl. trailing space (job_class attribute follows). Builder MLD: worktree TS commits need build:dist first (eslint.config self-ref); sonarjs/slow-regex rejects \s*\n?\s* in test regex — normalize whitespace then toContain; git rebase --autostash is the safe move with teammate-owned dirty files.

### T6 — Verification + verdict (lisa:verification-specialist) — DONE
C1-C4 all PASS empirically (check:plugins exit 0 clean tree; 10/10 artifact paths; AC-by-inspection incl. all defect spot-checks; 29/29 + frontmatter + typecheck). verification-status.json written with top-level in_progress pending C5. One doc nit fixed (plan line cited wrong base command path).

### T7 — PR + drive to merge + tracker sync + evidence — DONE
PR #1792 → main, auto-merge (merge commit), all 24 required checks green, MERGED 2026-07-20T01:18:56Z. Ancestry verified: merge commit 16aad418e parents = ebe18cd90 + b34915968 (our fixed head — no auto-merge race). Zero review threads. Issue #1744: status:done (terminal, single label), evidence comment with operator summary + AC matrix + Lisa Usage rows (source: unavailable), closed as completed. No parent → no rollup. Learnings-confirmation (3c.2): no ledger entries demonstrably applied in plan/diff — nothing to bump.

### T8 — Learnings capture (lisa:learner) — IN PROGRESS
learner-1744 spawned with 6 MLD candidates (3 from T3, 3 from T5); hostile-default judging; persists survivors via the executable contract only.

## Regression-spec note

Deliverable is agent-harness documentation (a skill), not user-visible product behavior with a UI/e2e surface. The project's e2e harness (Playwright) covers the console UI; there is no end-to-end harness that executes skills. Recorded per the no-harness exit: checked tests/e2e (UI only), plugin artifact tests (structural). The executable regression surface for THIS change is `check:plugins` + the plugin artifact/manifest test suite, which T3 keeps green and T6 proves.
