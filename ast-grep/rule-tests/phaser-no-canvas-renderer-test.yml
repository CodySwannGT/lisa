id: phaser-no-canvas-renderer
valid:
  - 'const config = { type: Phaser.AUTO, width: 800, height: 600 };'
  - 'const config = { type: Phaser.WEBGL };'
invalid:
  - 'const config = { type: Phaser.CANVAS, width: 800, height: 600 };'
