{
  "//": "Type-checks tests/, which tsconfig.json deliberately does not (CodySwannGT/lisa#3811). Run through scripts/check-typecheck-tests.mjs, never bare: the quarantine in typecheck-quarantine.json is what makes it green today, and tsc alone cannot read it.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": ".",
    "noEmit": true,
    "allowJs": true,
    "checkJs": false,
    "types": ["vitest/globals", "node"]
  },
  "include": ["src/**/*", "tests/**/*"],
  "exclude": [
    "node_modules",
    ".build",
    "dist",
    "src/opencode/plugin-templates"
  ],
  "files": []
}
