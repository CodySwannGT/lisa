# Prose surface (must be ignored)

Even in a project that has real duplicates, documentation examples are not
policy pins:

```bash
npm install -g @ast-grep/cli@0.40.4
```

The project currently runs on bun 1.3.8.
