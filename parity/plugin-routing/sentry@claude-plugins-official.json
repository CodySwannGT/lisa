{
  "schemaVersion": 1,
  "plugin": "sentry@claude-plugins-official",
  "pluginName": "sentry",
  "marketplace": "claude-plugins-official",
  "upstreamVersion": "1.4.0",
  "analyzedAt": "2026-09-02",
  "status": "approved",
  "components": [
    {
      "kind": "mcp",
      "id": "sentry",
      "path": ".claude-plugin/plugin.json",
      "classification": "mcp-server",
      "notes": "HTTP MCP server (https://mcp.sentry.dev/mcp?utm_source=plugin). As of 1.2.0 declared inline in plugin.json mcpServers only — one logical MCP component."
    },
    {
      "kind": "skill",
      "id": "sentry-create-alert",
      "path": "skills/sentry-create-alert/SKILL.md",
      "classification": "claude-skill",
      "notes": "Consolidated Sentry workflow skill (1.2.0 replaced the ~30 per-SDK skills; the 1.0.0 seer command is folded into sentry-debug-issue)."
    },
    {
      "kind": "skill",
      "id": "sentry-debug-issue",
      "path": "skills/sentry-debug-issue/SKILL.md",
      "classification": "claude-skill",
      "notes": "Consolidated Sentry workflow skill (1.2.0 replaced the ~30 per-SDK skills; the 1.0.0 seer command is folded into sentry-debug-issue)."
    },
    {
      "kind": "skill",
      "id": "sentry-feature-setup",
      "path": "skills/sentry-feature-setup/SKILL.md",
      "classification": "claude-skill",
      "notes": "Consolidated Sentry workflow skill (1.2.0 replaced the ~30 per-SDK skills; the 1.0.0 seer command is folded into sentry-debug-issue)."
    },
    {
      "kind": "skill",
      "id": "sentry-get-started",
      "path": "skills/sentry-get-started/SKILL.md",
      "classification": "claude-skill",
      "notes": "Consolidated Sentry workflow skill (1.2.0 replaced the ~30 per-SDK skills; the 1.0.0 seer command is folded into sentry-debug-issue). Re-reviewed at 1.4.0: this skill gained an onboarding progress protocol that reports staged setup progress to a third-party web application over the vendor's MCP, under an explicit instruction not to narrate those calls to the user. Lisa reviewed it and deliberately declined to reimplement it — data about the user's session leaving their environment as a silent side effect of running a skill is not a behavior Lisa adopts by default. The pin therefore records review at 1.4.0, not adoption of every change in the range; the reasoning lives in plugins/src/base/skills/lisa-parity-sentry-sdk-setup/SKILL.md."
    },
    {
      "kind": "skill",
      "id": "sentry-instrument",
      "path": "skills/sentry-instrument/SKILL.md",
      "classification": "claude-skill",
      "notes": "Consolidated Sentry workflow skill (1.2.0 replaced the ~30 per-SDK skills; the 1.0.0 seer command is folded into sentry-debug-issue)."
    },
    {
      "kind": "skill",
      "id": "sentry-otel-exporter-setup",
      "path": "skills/sentry-otel-exporter-setup/SKILL.md",
      "classification": "claude-skill",
      "notes": "Consolidated Sentry workflow skill (1.2.0 replaced the ~30 per-SDK skills; the 1.0.0 seer command is folded into sentry-debug-issue)."
    },
    {
      "kind": "skill",
      "id": "sentry-sdk-upgrade",
      "path": "skills/sentry-sdk-upgrade/SKILL.md",
      "classification": "claude-skill",
      "notes": "Consolidated Sentry workflow skill (1.2.0 replaced the ~30 per-SDK skills; the 1.0.0 seer command is folded into sentry-debug-issue)."
    },
    {
      "kind": "skill",
      "id": "sentry-setup-ai-monitoring",
      "path": "skills/sentry-setup-ai-monitoring/SKILL.md",
      "classification": "claude-skill",
      "notes": "Consolidated Sentry workflow skill (1.2.0 replaced the ~30 per-SDK skills; the 1.0.0 seer command is folded into sentry-debug-issue)."
    },
    {
      "kind": "skill",
      "id": "sentry-snapshots-cocoa",
      "path": "skills/sentry-snapshots-cocoa/SKILL.md",
      "classification": "claude-skill",
      "notes": "Consolidated Sentry workflow skill (1.2.0 replaced the ~30 per-SDK skills; the 1.0.0 seer command is folded into sentry-debug-issue)."
    },
    {
      "kind": "skill",
      "id": "sentry-workflow",
      "path": "skills/sentry-workflow/SKILL.md",
      "classification": "claude-skill",
      "notes": "Consolidated Sentry workflow skill (1.2.0 replaced the ~30 per-SDK skills; the 1.0.0 seer command is folded into sentry-debug-issue)."
    }
  ],
  "routing": {
    "codex": {
      "outcome": "re-point-mcp-lsp",
      "actions": [
        "emit the sentry HTTP MCP into Codex's .codex-plugin MCP pointer",
        "reimplement the 10 consolidated workflow skills as Lisa-native skills (Lisa consolidates further into lisa-parity-sentry-sdk-setup) stamped synced-from: sentry@claude-plugins-official@1.4.0 (or claude-only where a skill is Claude-specific)",
        "reimplement the sentry-debug-issue AI-debugging workflow (formerly the seer command) as a Lisa-native skill (lisa-parity-sentry-seer) stamped synced-from: sentry@claude-plugins-official@1.4.0"
      ],
      "rationale": "The dominant component is the sentry HTTP MCP, re-pointed into Codex's .codex-plugin pointer; the consolidated workflow skills (incl. the folded-in seer/debug workflow) are reimplemented as Lisa skills so no component group is dropped."
    },
    "cursor": {
      "outcome": "already-native",
      "actions": [
        "Cursor's variant emits mcp.json from .mcp.json, so the sentry MCP is already native",
        "the 10 consolidated skills load via Cursor's native .claude-plugin/ reading"
      ],
      "rationale": "Cursor reads .claude-plugin/ natively and its variant emits mcp.json from .mcp.json, so all component groups are already covered with no action."
    },
    "agy": {
      "outcome": "re-point-mcp-lsp",
      "actions": [
        "emit the sentry HTTP MCP via agy's user-global mcp-installer (src/agy/mcp-installer.ts)",
        "reimplement the 10 consolidated workflow skills as Lisa-native skills (Lisa consolidates further into lisa-parity-sentry-sdk-setup) stamped synced-from: sentry@claude-plugins-official@1.4.0 (or claude-only where Claude-specific)",
        "reimplement the sentry-debug-issue AI-debugging workflow (formerly the seer command) as a Lisa-native skill (lisa-parity-sentry-seer) stamped synced-from: sentry@claude-plugins-official@1.4.0"
      ],
      "rationale": "The dominant component is the sentry HTTP MCP, re-pointed via agy's mcp-installer; the consolidated workflow skills (incl. the folded-in seer/debug workflow) are reimplemented as Lisa skills (agy's fan-out does not carry curated third-party plugins)."
    },
    "copilot": {
      "outcome": "re-point-mcp-lsp",
      "actions": [
        "emit the sentry HTTP MCP as an inline mcpServers entry on Copilot's manifest",
        "reimplement the 10 consolidated workflow skills as Lisa-native skills (Lisa consolidates further into lisa-parity-sentry-sdk-setup) stamped synced-from: sentry@claude-plugins-official@1.4.0 (or enable Copilot's native equivalent where applicable)",
        "reimplement the sentry-debug-issue AI-debugging workflow (formerly the seer command) as a Lisa-native skill (lisa-parity-sentry-seer) stamped synced-from: sentry@claude-plugins-official@1.4.0"
      ],
      "rationale": "The dominant component is the sentry HTTP MCP, emitted inline on Copilot's manifest; the consolidated workflow skills (incl. the folded-in seer/debug workflow) are reimplemented as Lisa skills so no component group is dropped."
    }
  }
}
