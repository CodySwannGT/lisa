# bad-outcome@demo-marketplace — routing matrix (fixture)

Paired companion for the invalid routing-validate fixture. The `.md` exists so
the validator fails on the invalid `routing.codex.outcome` value, not on a
missing companion — keeping the exit-1 end-to-end assertion precise.
