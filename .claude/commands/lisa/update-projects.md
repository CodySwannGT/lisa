---
description: "Update local Lisa projects in batches — worktree per project, PR with auto-merge, auto-fix mergeability blockers, then Lisa self-upgrade"
allowed-tools: ["Skill"]
argument-hint: "(blank for all projects in .lisa.workspaces.json)"
---

Use the /lisa-update-projects skill to update local Lisa projects in batches. $ARGUMENTS
