---
description: "Fleet enforcement census — re-measure how many host checkouts resolve a Lisa enforcement guard at all, which copy governs, and its vintage. Reports; never gates."
allowed-tools: ["Skill"]
argument-hint: "(blank for the roster; --scan <dir> to discover; --redact before quoting publicly)"
---

Use the /lisa-enforcement-census skill to re-measure fleet enforcement coverage. $ARGUMENTS
