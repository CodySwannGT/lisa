{
  "force": {
    "scripts": {
      "prepublishOnly": "$npm_execpath run build",
      "prepare": "$npm_execpath run build || husky install || true"
    }
  },
  "defaults": {
    "files": [
      "dist"
    ]
  }
}
