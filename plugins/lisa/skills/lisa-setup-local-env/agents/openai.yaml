display_name: "Setup Local Env"
short_description: "Bring a developer's machine in line with the toolchain the project declares"
default_prompt:
  - "Use $lisa-setup-local-env: Bring a developer's machine in line with the toolchain the project declares."
