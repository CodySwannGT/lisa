---
description: "Bootstrap a machine before any repository exists. Detects Lisa-supported agent CLIs plus universal tooling, and installs missing tools only with --yes."
allowed-tools: ["Skill"]
argument-hint: "[--yes] [--agents=claude,codex,cursor,opencode,agy,copilot] [--json] [--dry-run]"
---

Use the /lisa-setup-workstation skill to prepare this machine before cloning a project. $ARGUMENTS

