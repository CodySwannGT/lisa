# This file is managed by Lisa and IS replaced on each `lisa` run.
# Do not edit directly — durable changes belong upstream in Lisa.

pre-commit:
  parallel: true
  commands:
    rubocop:
      glob: '*.rb'
      run: bundle exec rubocop --force-exclusion {staged_files}
    threshold-ratchet:
      # Quality thresholds (simplecov minimums, rubocop Metrics maximums, …)
      # may tighten, never weaken. Same deterministic comparator as the
      # agent-time hooks and CI; self-skips when node or the script is absent
      # (CI still enforces the gate).
      run: sh -c 'command -v node >/dev/null 2>&1 || exit 0; [ -f scripts/check-threshold-ratchet.mjs ] || exit 0; node scripts/check-threshold-ratchet.mjs --staged'
    bundler-audit:
      # The wrapper removes Git's complete documented local environment before
      # `--update` enters the nested ruby-advisory-db repository.
      run: sh scripts/lisa-clean-git-env.sh bundle exec bundler-audit check --update

prepare-commit-msg:
  commands:
    work-item-trailer:
      run: node scripts/lisa-work-item.mjs prepare-commit-msg {1} {2} {3}

commit-msg:
  commands:
    work-item:
      run: node scripts/lisa-work-item.mjs validate-commit {1}
    conventional-commit:
      run: 'head -1 {1} | grep -qE "^(feat|fix|chore|docs|style|refactor|perf|test|build|ci|revert)(\(.+\))?: .+" || (echo "Commit message must follow conventional format" && exit 1)'

pre-push:
  commands:
    work-item:
      run: node scripts/lisa-work-item.mjs validate-push {1}
      use_stdin: true
    rspec:
      # One supervisor boundary: run-scoped scratch, an outside cleanup
      # authority armed before the payload allocates anything, and no debris
      # left behind on any ending.
      run: sh scripts/lisa-clean-git-env.sh sh scripts/lisa-scratch-run.sh --suite prepush-rspec -- bundle exec rspec
    brakeman:
      run: sh scripts/lisa-clean-git-env.sh bundle exec brakeman --no-pager --quiet
    reek:
      run: sh scripts/lisa-clean-git-env.sh bundle exec reek app/ lib/
    flog:
      run: sh scripts/lisa-clean-git-env.sh bundle exec flog --all --group app/ lib/
    flay:
      run: sh scripts/lisa-clean-git-env.sh bundle exec flay app/ lib/
    mutation:
      # Opt-in, diff-only mutation gate. Self-skips instantly when disabled
      # (mutation.gate.yml: enabled: false), so it adds no cost until opted in.
      # The supervisor wraps the gate script rather than living inside it, so
      # this route and the CI route each cross exactly one boundary.
      run: sh scripts/lisa-clean-git-env.sh sh scripts/lisa-scratch-run.sh --suite prepush-mutant -- bash scripts/lisa-mutation.sh
